<div id="wrapper_my_content">

  <div class="blog_background">
    <% if @user.background.attached? %>
    <%= image_tag(@user.background.variant(resize_to_limit:[200,200])) %>
    <% end %>
  </div>

  <div class="container_blog_load">
    <div class="blog_load">
      <div class="container_blog_name">
        <p class="new_blog">New blog</p>
          <p class="corp_name">minato no-en presents</p>
          <h1 class="blog_name">
            <%= @user.blog_name %>
          </h1>
          <div class="title_image">
            <% if @user.title_image.attached? %>
              <%= image_tag(@user.title_image.variant(resize_to_limit:[150,150])) %>
            <% else %>
              <%= "" %>
            <% end %>
          </div>
      </div>

        <div class="container_blog_f">
          <%= form_with model: @blog, url: blogs_my_content_index_path do |f| %>
            <div class="title_blog">
              <p class="chara">タイトル（必須）</p>
              <%= f.text_field :title, placeholder: '20字以内' %>
            </div>
            <% if @blog.errors.messages[:title].any? %>
              <p class="error"><%= "タイトルを入力して下さい。" %></p>
            <% end %>
            <div class="caption_blog">
              <p class="chara">見出しイメージ</p>
              <%= f.file_field :caption_image, placeholder: '写真・絵など' %>
            </div>
            <div class="content_blog">
              <p class="chara">内容（必須）</p>
              <%= f.rich_text_area :content %>
            </div>
            <% if @blog.errors.messages[:content].any? %>
              <p class="error"><%= "内容を入力して下さい。" %></p>
            <% end %>
            <div class="submit">
              <%= f.submit %>
            </div>
          <% end %>
        </div>

        <div class="back_to_top"><%= link_to "Topに戻る", root_path %></div>
      
    </div>
  </div>
</div>
